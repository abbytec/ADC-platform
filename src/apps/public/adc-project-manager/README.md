# adc-project-manager

Panel de gestión de proyectos tipo Jira: proyectos, sprints, milestones, issues.

- Subdominio: `projects.adigitalcafe.com`
- devPort: 3018
- Consume `ProjectManagerService` via `/api/pm`
- Usa `IdentityManagerService` para resolver usuarios/grupos en pickers
